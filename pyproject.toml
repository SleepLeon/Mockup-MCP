[project]
name = "steering-mcp"
version = "0.1.0"
description = "Steering assistant MCP server"
authors = [
    {name = "Leon Brochmann",email = "leon.brochmann@emma-sleep.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli] (>=1.27.1,<2.0.0)",
]


[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
